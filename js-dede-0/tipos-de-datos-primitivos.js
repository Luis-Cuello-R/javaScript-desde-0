// Declaración de variables con valores booleanos
const isActive = true
const hasPermission = false

// Comparación que devuelve un valor booleano
const result = 5 > 3
console.log(result) // Imprime: true, ya que 5 es mayor que 3

// Conversión implícita a booleano utilizando la doble negación (!!)
const name = "Platzi"
console.log(!!name) // Imprime: true, ya que el string "Platzi" no está vacío y, por lo tanto, es verdadero

// Conversión explícita a booleano
const value = 0
const otherValue = 25;

// Conversión explícita a booleano utilizando la función Boolean()
const explicitBoolean = Boolean(value)

// Imprime el valor de explicitBoolean, que representa la conversión booleana de 'value'
console.log(explicitBoolean) // Imprime: false, ya que 0 se considera falso en JavaScript

```// Declaración de variables con valores booleanos

const isActive = true

const hasPermission = false



// Comparación que devuelve un valor booleano

const result = 5 > 3

console.log(result) // Imprime: true, ya que 5 es mayor que 3



// Conversión implícita a booleano utilizando la doble negación (!!)

const name = "Platzi"

console.log(!!name) // Imprime: true, ya que el string "Platzi" no está vacío y, por lo tanto, es verdadero



// Conversión explícita a booleano

const value = 0

const otherValue = 25



// Conversión explícita a booleano utilizando la función Boolean()

const explicitBoolean = Boolean(value)



// Imprime el valor de explicitBoolean, que representa la conversión booleana de 'value'

console.log(explicitBoolean) // Imprime: false, ya que 0 se considera falso en JavaScript